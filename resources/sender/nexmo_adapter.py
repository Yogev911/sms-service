import nexmo
# import conf


def send(src, dest, msg):
    # client = nexmo.Client(key='3fdf633b', secret='mVZW9LHrKGS7IFn7')
    # client.send_message({
    #     'from': src,
    #     'to': f'972{dest}',
    #     'text': msg,
    # }
    # )
    print('sms sent!')
